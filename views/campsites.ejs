<% include partials/header %>

<h1>This is the Camp Sites page</h1>

<!-- <% campsites.forEach(function(site) { %>
    <div>
        <h3><%= site.name %></h3>
        <img src="<%= site.image %>" alt="camp site image" />
    </div>
<% }); %> -->

<% include partials/footer %>
